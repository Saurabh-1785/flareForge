# Relayer / Event Watcher

> **Layer 4** — not built yet.

Go service with goroutine-per-vault deadline watching. Watches on-chain events, drives reminders, and shuttles enclave results to `submitQuorumResult()`.
